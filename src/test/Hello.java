package test;

public class Hello implements Runnable
{
	public Hello()
	{		
		/*
		try
		{
		m_client = new Socket("127.0.0.1", 8080);
		//InetSocketAddress inetAddress = new InetSocketAddress(, port);
		//m_client.connect(inetAddress);
		if(m_client.isConnected())
		{
			System.out.println("connected");
		}
		}
		catch(IOException e)
		{
			System.out.println(e);
			e.printStackTrace();	
		}
		// new Thread(this).start();
		*/
		
	}

	public void run()
	{
		while(true)
		{
		/*try
		{
			//InputStream ins = m_client.getInputStream();
			// ret = ins.read();
			//System.out.printf("%d", ret);
		}
		catch(IOException e)
		{
			System.out.println(e);
			e.printStackTrace();	
		}*/
		}
	}
	//private Socket m_client;
}
